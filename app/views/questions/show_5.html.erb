<h1>Q5</h1>
<!--フォームに入力されたデータをDBへ保存しないので次の遷移ページにurlのパスを指定する-->
<%= form_with url: show_6_questions_path do |form| %>
  <!--questions_controllerで設定した@questions = Question.find(9)のcontentを表示する-->
  <%= @question.content %><br>
  <!--:gymはformで渡すデータのnameを指定する。@answers_9にはAnswer.where(question_id: Q_09)が格納されている-->
  <%= form.collection_select(:gym, @answers_9, :content, :content, {prompt: "どちらか選択してください"}, required: true) %><br>
<br>
  <%= form.submit 'NEXT', class: "btn btn-primary" %>
<% end %>

<%= link_to 'BACK', show_4_questions_path %>
