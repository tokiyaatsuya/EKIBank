<h1>Q10</h1>
<h2>駅前に23時まで営業しているスーパーマーケットは必須？</h2>

<%= form_for @question, url: show11_questions_path,　method: :get do |f| %>
  <div class="form-group">
    <%= f.select :supermarket, {
      '必須': 1,
      '必須ではない': 2,
    }, { include_blank: 'どちらか選択してください' }, { class: 'form-control' } %>
  </div>
  <div class="form-group">
    <%= f.submit 'NEXT', class: 'btn btn-primary btn-block' %>
  </div>
<% end %>
  
<%= link_to 'BACK', :back, class: 'btn btn-primary' %>
