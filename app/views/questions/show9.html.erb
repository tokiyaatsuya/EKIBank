<h1>Q9</h1>
<h2>駅前に食べログの評価が3.5以上のお店は必須？</h2>

<%= form_for @question, url: show10_questions_path,　method: :get do |f| %>
  <div class="form-group">
    <%= f.select :tabelog_high_score, {
      '必須': 1,
      '必須ではない': 2,
    }, { include_blank: 'どちらか選択してください' }, { class: 'form-control' } %>
  </div>
  <div class="form-group">
    <%= f.submit 'NEXT', class: 'btn btn-primary btn-block' %>
  </div>
<% end %>
  
<%= link_to 'BACK', :back, class: 'btn btn-primary' %>