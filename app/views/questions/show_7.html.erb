<body class="questions-show-7">
  <%= stylesheet_pack_tag 'questions_show_7', media: 'all', 'data-turbolinks-track': 'reload' %>
  <div class="container">
    <div class="text-center">
      <h1>Q7</h1>
      <!--フォームに入力されたデータをDBへ保存しないので次の遷移ページにurlのパスを指定する-->
      <%= form_with url: show_8_questions_path do |form| %>
        <!--questions_controllerで設定した@questions = Question.find(11)のcontentを表示する-->
        <p><%= @question.content %></p>
        <!--:mcdonaldsはformで渡すデータのnameを指定する。@answers_11にはAnswer.where(question_id: Q_11)が格納されている-->
        <h3><%= form.collection_select(:mcdonalds, @answers_11, :content, :content, {prompt: "どちらか選択してください"}, required: true) %></h3>
      <br>
        <%= form.submit 'NEXT', class: "btn btn-warning btn-lg" %>
      <% end %>
      <br>
      <!--質問回答数をプログレスバーを部分テンプレート化して表示する-->
      <p>質問回答数 6/12 問</p>
      <%= render partial: 'layouts/progress_bar', locals: { style: "width: 49.5%" } %>
    </div>
  </div>
</body>