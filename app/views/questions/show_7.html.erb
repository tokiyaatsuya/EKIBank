<h1>Q7</h1>
<!--フォームに入力されたデータをDBへ保存しないので次の遷移ページにurlのパスを指定する-->
<%= form_with url: show_8_questions_path do |form| %>
  <!--questions_controllerで設定した@questions = Question.find(10)のcontentを表示する-->
  <%= @question.content %><br>
  <!--:starbucks_coffeeはformで渡すデータのnameを指定する。@answers_10にはAnswer.where(question_id: Q_10)が格納されている-->
  <%= form.collection_select(:starbucks_coffee, @answers_10, :content, :content, {prompt: "どちらか選択してください"}) %><br>
<br>
  <%= form.submit 'NEXT' %>
<% end %>

<%= link_to 'BACK', show_6_questions_path %>
