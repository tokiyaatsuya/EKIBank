<h1>Q1</h1>
<!--フォームに入力されたデータをDBへ保存しないので次の遷移ページにurlのパスを指定する-->
<%= form_with url: show_2_questions_path do |form| %>
    <p>賃貸派</p>
      <!--questions_controllerで設定した@questions = Question.find(1,2,3,18)から指定した配列番号のcontentを表示する-->
      <%= @questions[0]['content'] %><br>
      <!--:rent_budgetはformで渡すデータのnameを指定する。@answers_1にはAnswer.where(question_id: Q_01)が格納されている-->
      <%= form.collection_select(:rent_budget, @answers_1, :content, :content, {prompt: "家賃の予算を選択してください"}, required: true) %><br>
    <br>
      <!--questions_controllerで設定した@questions = Question.find(1,2,3,18,19)から指定した配列番号のcontentを表示する-->
      <%= @questions[3]['content'] %><br>
      <!--:floor_planはformで渡すデータのnameを指定する。@answers_18にはAnswer.where(question_id: Q_18)が格納されている-->
      <%= form.collection_select(:floor_plan, @answers_18, :content, :content, {prompt: "希望する間取りを選択してください"}, required: true) %><br>
    <br>
    <!--<p>購入派</p>-->
      <!--questions_controllerで設定した@questions = Question.find(1,2,3,18,19)から指定した配列番号のcontentを表示する-->
      <!--<%= @questions[1]['content'] %><br>-->
      <!--:buy_typeはformで渡すデータのnameを指定する。@answers_2にはAnswer.where(question_id: Q_02)が格納されている-->
      <!--<%= form.collection_select(:buy_type, @answers_2, :id, :content, {prompt: "購入予定の不動産の種別を選択してください"}) %><br>-->
    <!--<br>-->
      <!--questions_controllerで設定した@questions = Question.find(1,2,3,18,19)から指定した配列番号のcontentを表示する-->
      <!--<%= @questions[2]['content'] %><br>-->
      <!--:buy_budgetはformで渡すデータのnameを指定する。@answers_3にはAnswer.where(question_id: Q_03)が格納されている-->
      <!--<%= form.collection_select(:buy_budget, @answers_3, :id, :content, {prompt: "購入予定の不動産の予算を選択してください"}) %><br>-->
    <br>
      <%= form.submit 'NEXT', class: "btn btn-primary" %>
<% end %>
